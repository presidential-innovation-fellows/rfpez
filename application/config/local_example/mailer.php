<?php

return array(
  // uncomment the next line to enable sending of email
  // 'transport' => Swift_SmtpTransport::newInstance(),

  // redirect all email to a single email address
  'send_all_to' => array('foo@bar.com')
);