- Section::inject('page_title', 'Show Bid')
- Section::inject('no_page_header', true)
- Section::inject('active_nav', 'contracts')

- if ($bid->project->is_mine()):
  !{View::make('bids.dismiss_modal')}
- endif;

!{View::make('bids.partials.bid_details')->with('bid', $bid)}