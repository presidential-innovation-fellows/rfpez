- Section::inject('page_title', "Review Bids for $contract->title")

- if (!Input::get('show_all')):
  a(href="#{route('bids', array($contract->id))}?show_all=true") show all bids (including dismissed)
- else:
  a(href="#{route('bids', array($contract->id))}") hide dismissed bids
- endif;

!{View::make('bids.dismiss_modal')}

- foreach($bids as $bid):
  .bid
    | $#{intval($bid->total_price())} - #{$bid->vendor->company_name}
    |
    a(href="#{route('bid', array($contract->id, $bid->id))}") details
    | |

    - if($bid->dismissed()):
      dismissed
    - else:
      a(href="#", class="show-dismiss-modal", data-contract-id="#{$contract->id}", data-bid-id="#{$bid->id}", data-vendor-company-name="#{$bid->vendor->company_name}", data-remove-from-list="bid") Dismiss?
