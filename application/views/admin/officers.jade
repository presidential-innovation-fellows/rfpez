- Section::inject('no_page_header', true)

!{View::make('admin.partials.subnav')->with('current_page', 'officers')}

table.table.table-bordered.table-striped.admin-officers-table
  thead
    tr
      th id
      th name
      th title
      th email
      th role
      th actions

  tbody#officers-tbody
    :javascript
      | $(function(){
      |  new Rfpez.Backbone.AdminOfficers( !{$officers_json} )
      | })

.pagination-wrapper
  !{$officers->links()}