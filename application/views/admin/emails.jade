.subheader
  - Section::inject('no_page_header', true)
  h4 Admin Panel

div.container.inner-container

	!{View::make('admin.partials.subnav')->with('current_page', 'emails')}

	h5 Vendors
	p #{implode(', ', $vendor_emails)}

	h5 Officers
	p #{implode(', ', $officer_emails)}