- Section::inject('page_title', 'Notifications')

- if ($notifications):
  ul.notification-list
    - foreach ($notifications as $notification):
      !{View::make('partials.media.notification')->with('notification', $notification)}
- else:
  p No notifications to show.
- endif;
