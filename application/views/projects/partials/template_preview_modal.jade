.modal.hide.template-preview-modal(tabindex="-1", role="dialog")
  .modal-header
    button.close(type='button', data-dismiss='modal') &times;
    h3 #{$template->title}
  .modal-body

    !{View::make('projects.partials.full_sow')->with('project', $template)}

  .modal-footer
    button.btn(data-dismiss="modal") Close
