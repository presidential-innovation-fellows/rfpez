.selected-sections
  - if ($sections_by_category = $project->sections_by_category()):
    - foreach ($sections_by_category as $category => $sections):
      strong #{$category}

      - foreach ($sections as $section):
        .section(data-section-id="#{$section->id}") &bullet; #{$section->title}
          a.btn.btn-mini.remove-button(data-href="#{route('project_section_delete', array($project->id, $section->id))}", data-loading-text="Removing...")
            i.icon-trash
  - else:
    p No sections have been added yet.
  - endif;