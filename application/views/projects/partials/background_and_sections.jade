h3.sow-section-header.sow-section-header-first Background
p !{$project->background}

- foreach ($project->sections_by_category() as $category => $sections):
  h3.sow-section-header #{$category}

  - foreach ($sections as $section):
    strong #{$section->title}
    p !{$section->body}
