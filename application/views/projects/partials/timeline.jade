h5 Timeline

table.table.timeline-table
  thead
    tr
      th Deliverable
      th Completion Date
  tbody
    - if ($project->deliverables):
      - foreach ($project->deliverables as $deliverable => $date):
        tr
          td #{$deliverable}
          td #{$date}