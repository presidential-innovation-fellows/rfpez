- Section::inject('page_title', $project->title)
- Section::inject('page_action', "Edit Sections")
- Section::inject('active_subnav', 'create')
- Section::inject('active_sidebar', 'edit_sections')
- Section::inject('no_page_header', true)

!{View::make('projects.partials.toolbar')->with('project', $project)}
!{View::make('projects.partials.edit_section_modal')->with('project', $project)}

button.btn.btn-success.pull-right.add-section-button Add Section
  i.icon-white.icon-plus-sign

.row-fluid
  .span3
    !{View::make('projects.partials.sow_composer_sidebar')->with('project', $project)}

  .span9
    !{View::make('projects.partials.sections_for_editing')->with('project', $project)}

    .form-actions
      a.btn.btn-primary(href="#{route('project_blanks', array($project->id))}") Fill in Blanks &rarr;
